developを用意する
ローカルのメインを基本選択していればOK
ローカルのメインでフェッチをしたら、メインにツリー構造が現れる
mainからdevelopをつくる
main＞コマンド入力欄＞いかにブランチを作成＞作成したいツリーを選ぶ＞今回はdevelopを入力＞pullを押す
developブランチに、昨日ごとのブランチを作る
(develop-featureの記事を参照する)
命名規則は最初小文字-最初小文字
自分の変更したファイルをステージング＞コミット
コミットを押したら自動的にプッシュしてくれる。
いつも「コミット」が書いてあるボタンが「雲のマークとBranch作成」に代わっているので、そこを押すと、ローカルリポジトリで作ったfeature/delete-thumbがリモートリポジトリに反映される。

マージがコミットになる＜あとでしらべてね、みくちゃん。はい。